README

In command line run:

% chmod 775 run.sh
%./run.sh [seed]

Just replace [seed] with desired seed, no punctuation around it. privKey.txt is a new file being created to store the new ETH private key.



